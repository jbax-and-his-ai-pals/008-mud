__pycache__
concatenated/
*.sqlite3
*.sqlite3-journal
.godot/
server/data/saves/
server/tests.log
tmp/
server/tests/_tmp/
# Recovery state the editor writes before a multi-file save: timestamped copies of
# a set's `data/` tree, kept per machine rather than shared with the project.
content_sets/*/editor/checkpoints/
.venv/
.DS_Store
server/.coverage
server/.coverage.*
coverage_html/